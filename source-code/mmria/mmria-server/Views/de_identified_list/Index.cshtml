@using System.Security.Claims;
@{
    Layout = "_LayoutBase";

    ViewBag.BreadCrumbs = true;
    ViewBag.Title = "De-identified Line List Summary";

    var is_cdc_admin = false;
    var is_abstractor = false;

    var userName = "";
  
    if (User.Identities.Any(u => u.IsAuthenticated))
    {
        userName = User.Identities.First(
        u => u.IsAuthenticated && 
        u.HasClaim(c => c.Type == ClaimTypes.Name)).FindFirst(ClaimTypes.Name).Value;
    }

    foreach(var role in User.Identities.First(u => u.IsAuthenticated &&  u.HasClaim(c => c.Type == ClaimTypes.Name)).Claims.Where(c=> c.Type == ClaimTypes.Role))
    {
      switch(role.Value)
      {
        case "abstractor":
          is_abstractor = true;
          break;
        case "cdc_admin":
          is_cdc_admin = true;
          break;
      }
    }




}

@section HeadScripts
{
    <script src="../scripts/mmria.js" type="text/javascript"></script>
    <script src="./scripts/de-identified-list/index.js"></script>
    <script>
 


function main()
{

  //print_node(metadata);
}

function print_node(p_node)
{
  console.log(p_node.name);
  console.log(p_node.prompt);
  console.log(p_node.type);

  if(p_node.children)
  {
    for(var i in p_node.children)
    {
      var child = p_node.children[i];
      print_node(child);
    }
  }

}

</script>
}

<h1 tabindex="-1">Committee Member De-Identified List</h1> <button class="btn btn-primary mt-3 d-none">Clear All TEST Data</button>
@section styles {
    <link rel="stylesheet" href="/css/index.css">
}


<div id="output">Manage de identified list</div>
