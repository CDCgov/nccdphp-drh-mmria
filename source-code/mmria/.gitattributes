# Set the default behavior to automatically handle line endings.
* text=auto

# Explicitly set line endings for specific file types if necessary.
# For example, enforce 'LF' for all shell scripts.
*.sh text eol=lf